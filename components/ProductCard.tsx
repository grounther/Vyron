import Link from 'next/link'
import { Product } from '@/lib/products'
export default function ProductCard({p}:{p:Product}){return <Link href={`/product/${p.slug}`} className="card group block overflow-hidden rounded-3xl transition hover:-translate-y-1 hover:border-white/20"><div className="overflow-hidden bg-white/5"><img src={p.hero} alt={p.name} className="product-img opacity-85 transition duration-500 group-hover:scale-105 group-hover:opacity-100"/></div><div className="p-4 md:p-5"><div className="mb-3 inline-flex rounded-full bg-white/10 px-3 py-1 text-[11px] font-bold uppercase tracking-wider text-white/60">{p.badge}</div><h3 className="line-clamp-2 text-base font-black md:text-lg">{p.name}</h3><p className="mt-2 line-clamp-2 text-sm text-white/55">{p.short}</p><div className="mt-5 flex items-end justify-between gap-3"><div><span className="text-lg font-black md:text-xl">€{p.price.toFixed(2)}</span>{p.compareAt&&<span className="ml-2 text-xs text-white/35 line-through">€{p.compareAt.toFixed(2)}</span>}</div><span className="rounded-full bg-white px-3 py-2 text-xs font-black text-black">View</span></div></div></Link>}
