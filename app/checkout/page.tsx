import CheckoutClient from './CheckoutClient'

export const metadata = { title: 'Checkout | ASORTA' }

export default function CheckoutPage() {
  return <main className="mx-auto max-w-7xl px-4 py-10 md:px-6"><CheckoutClient /></main>
}
