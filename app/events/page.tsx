import EventCard from '@/components/EventCard'
import { events } from '@/lib/tickets'
import { Search } from 'lucide-react'
export const metadata={title:'Evenementen'}
export default function EventsPage(){return <main className="mx-auto min-h-screen max-w-7xl px-4 py-14 sm:px-5"><p className="text-xs font-black uppercase tracking-[.25em] text-[#b8ff5a]">Heel Nederland</p><h1 className="mt-3 text-4xl font-black sm:text-6xl">Vind jouw volgende evenement</h1><div className="mt-8 flex items-center gap-3 rounded-2xl border border-white/10 bg-white/[.04] px-4 py-3"><Search size={19} className="text-white/38"/><input className="w-full bg-transparent text-sm outline-none placeholder:text-white/30" placeholder="Zoek op artiest, evenement, locatie of plaats" aria-label="Evenementen zoeken"/></div><div className="mt-5 flex flex-wrap gap-2">{['Alles','Concert','Festival','Dance','Sport','Theater'].map((x,i)=><button key={x} className={`rounded-full px-4 py-2 text-sm font-black ${i===0?'bg-[#b8ff5a] text-black':'border border-white/10 text-white/58 hover:bg-white/8'}`}>{x}</button>)}</div><div className="mt-9 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{events.map(event=><EventCard key={event.slug} event={event}/>)}</div></main>}
