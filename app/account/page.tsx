import Link from 'next/link'
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { CalendarDays,Heart,TicketCheck,WalletCards } from 'lucide-react'
export const dynamic='force-dynamic'
export default async function AccountPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login?next=/account');const cards=[{Icon:TicketCheck,label:'Mijn tickets',value:'0'},{Icon:WalletCards,label:'Aangeboden',value:'0'},{Icon:CalendarDays,label:'Evenementen',value:'0'},{Icon:Heart,label:'Favorieten',value:'0'}];return <main className="mx-auto min-h-screen max-w-6xl px-4 py-14 sm:px-5"><p className="text-xs font-black uppercase tracking-[.25em] text-[#b8ff5a]">Mijn ASORTA</p><h1 className="mt-3 text-4xl font-black sm:text-6xl">Welkom terug.</h1><p className="mt-3 text-white/45">{user.email}</p><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(({Icon,label,value})=><div key={label} className="rounded-2xl border border-white/10 bg-white/[.035] p-5"><Icon className="text-[#b8ff5a]"/><strong className="mt-7 block text-3xl">{value}</strong><span className="text-sm text-white/42">{label}</span></div>)}</div><div className="mt-8 rounded-[2rem] border border-white/10 bg-white/[.035] p-7"><h2 className="text-2xl font-black">Nog geen tickets</h2><p className="mt-3 text-white/48">Gekochte tickets en actieve advertenties verschijnen straks hier.</p><div className="mt-6 flex flex-wrap gap-3"><Link href="/events" className="rounded-full bg-[#b8ff5a] px-5 py-3 font-black text-black">Zoek evenementen</Link><Link href="/sell" className="rounded-full border border-white/12 px-5 py-3 font-black">Ticket verkopen</Link><form action="/auth/signout" method="post"><button className="rounded-full border border-white/12 px-5 py-3 font-black text-white/52">Uitloggen</button></form></div></div></main>}
